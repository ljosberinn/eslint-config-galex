const prefix = 'eslint-config-galex';

const jestOverrideType = `${prefix}/jest`;
const reactOverrideType = `${prefix}/react`;
const tsOverrideType = `${prefix}/typescript`;
const nextOverrideType = `${prefix}/next`;

module.exports = {
  jestOverrideType,
  reactOverrideType,
  tsOverrideType,
  nextOverrideType,
};
